apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}-svc-stage
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.appName }}
    {{- if eq .Values.productionSlot "blue" }}
    slot: green
    {{- else }}
    slot: blue
    {{- end }}
  ports:
    - protocol: TCP
      name: http
      port: {{ .Values.service.port }}

{{- if .Values.ingress.enabled }}
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ .Values.service.name }}-stage
  labels:
    app: {{ .Values.service.name }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
  annotations:
    {{- range $key, $value := .Values.ingress.annotations }}
      {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  rules:
    {{- range $key, $value := .Values.ingress.rules }}
    - host: stage{{ $value.host }}
      http:
        paths:
        {{- range $path := .paths }}
          - pathPrefix: {{ $path.pathPrefix }}
            backend:
              serviceName: {{ $path.serviceName }}-svc-stage
              servicePort: {{ $path.servicePort }}
        {{- end -}}
    {{- end -}}
  {{- if .Values.ingress.tls }}
  tls:
{{ toYaml .Values.ingress.tls | indent 4 }}
  {{- end }}
{{- end }}